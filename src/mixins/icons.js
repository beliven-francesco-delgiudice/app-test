import {
  mailOutline,
  keyOutline,
  personOutline,
  chevronBackOutline,
  documentTextOutline,
  documentAttachOutline,
  lockOpenOutline
} from 'ionicons/icons'

export default {
  created () {
    this.icons = {
      mailOutline,
      keyOutline,
      personOutline,
      chevronBackOutline,
      documentTextOutline,
      documentAttachOutline,
      lockOpenOutline
    }
  }
}
